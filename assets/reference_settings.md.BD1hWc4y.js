import{_ as s,E as d,c,m as t,a as e,J as a,a4 as i,o as n}from"./chunks/framework.DTvtauHd.js";const A=JSON.parse('{"title":"Settings","description":"","frontmatter":{"title":"Settings"},"headers":[],"relativePath":"reference/settings.md","filePath":"reference/settings.md","lastUpdated":1713138845000}'),r={name:"reference/settings.md"},l=i('<h1 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h1><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>This page may contain outdated or incomplete information. You can see a description of most available settings in the default settings file (<strong>Preferences → Settings</strong> or <code>Default/Preferences.sublime-settings</code>).</p></div><div class="info custom-block"><p class="custom-block-title">See Also</p><p><a href="/guide/customization/settings.html">Settings User Guide</a> : Explanations for how settings work in Sublime Text.</p></div><h2 id="global-settings" tabindex="-1">Global Settings <a class="header-anchor" href="#global-settings" aria-label="Permalink to &quot;Global Settings&quot;">​</a></h2><p>These settings can only be modified from <code>Preferences.sublime-settings</code> and <code>Preferences ({platform}).sublime-settings</code>.</p><p><code>theme</code> : Theme to be used. Accepts a file base name (e. g.: <code>Default.sublime-theme</code>).</p><p><code>scroll_speed</code> : Set to <code>0</code> to disable smooth scrolling. Set to a value between <code>0</code> and <code>1</code> to scroll slower, or set to a value larger than <code>1</code> to scroll faster.</p><p><code>hot_exit</code> : Exiting the application or window with an associated project with <code>hot_exit</code> enabled will cause it to close immediately without prompting. Unsaved modifications and open files will be preserved and restored when next starting.</p><p><code>remember_open_files</code> : Determines whether to reopen the buffers that were open when Sublime Text was last closed.</p><p><code>open_files_in_new_window</code> : macOS only. When filters are opened from Finder, or by dragging onto the dock icon, this controls if a new window is created or not.</p><p><code>close_windows_when_empty</code> : Close windows as soon as the last file is closed, unless there&#39;s a folder open within the window.</p><p><code>show_full_path</code> : Show the full path to files in the title bar.</p><p><code>preview_on_click</code> : If <code>true</code>, preview file contents when clicking on a file in the side bar. Double clicking or editing the preview will open the file and assign it a tab.</p><p><code>folder_exclude_patterns</code> : Excludes the matching folders from the side bar, GoTo Anything, etc.</p><p><code>file_exclude_patterns</code> : Excludes the matching files from the side bar, GoTo Anything, etc.</p><p><code>binary_file_patterns</code> : Excludes the matching files from GoTo Anything and Find in Files but not the side bar.</p><p><code>show_tab_close_buttons</code> : If <code>false</code>, hides the tabs&#39; close buttons until the mouse hovers over the tab.</p><p><code>mouse_wheel_switches_tabs</code> : If <code>true</code>, scrolling the mouse wheel will cause tabs to switch if the cursor is in the tab area.</p><p><code>open_files_in_new_window</code> : macOS only. When filters are opened from Finder, or by dragging onto the dock icon, this controls whether a new window is created or not.</p><p><code>ignored_packages</code> : A list of packages that will be ignored (not loaded).</p><h2 id="file-settings" tabindex="-1">File Settings <a class="header-anchor" href="#file-settings" aria-label="Permalink to &quot;File Settings&quot;">​</a></h2><h3 id="whitespace-and-indentation" tabindex="-1">Whitespace and Indentation <a class="header-anchor" href="#whitespace-and-indentation" aria-label="Permalink to &quot;Whitespace and Indentation&quot;">​</a></h3><p><code>auto_indent</code> : Toggles automatic indentation.</p><p><code>tab_size</code> : Number of spaces a tab is considered equal to.</p>',24),p=t("code",null,"translate_tabs_to_spaces",-1),h=t("code",null,"tab_size",-1),u=t("code",null,"use_tab_stops",-1),_=t("code",null,"translate_tabs_to_spaces",-1),f=t("code",null,"true",-1),g=t("code",null,"tab_size",-1),m=i('<p><code>trim_automatic_white_space</code> : Toggles deletion of white space added by <code>auto_indent</code>.</p><p><code>detect_indentation</code> : Set to <code>false</code> to disable detection of tabs vs. spaces whenever a buffer is loaded. If set to <code>true</code>, it automatically will modify <code>translate_tabs_to_spaces</code> and <code>tab_size</code>.</p><p><code>draw_white_space</code> : Valid values: <code>none</code>, <code>selection</code>, <code>all</code>.</p><p><code>trim_trailing_white_space_on_save</code> : Set to <code>true</code> to remove white space on save.</p><h3 id="visual-settings" tabindex="-1">Visual Settings <a class="header-anchor" href="#visual-settings" aria-label="Permalink to &quot;Visual Settings&quot;">​</a></h3><p><code>always_show_minimap_viewport</code> : If set to true, then it will always show rectangle on minimap highlighting current document position; defualt false, which shows position only on mouse over the minimap.</p><p><code>color_scheme</code> : Sets the colors used for text highlighting. Accepts a path rooted at the data directory (e.g.: <code>Packages/Color Scheme - Default/Monokai Bright.tmTheme</code>).</p><p><code>font_face</code> : Font face to be used for editable text.</p><p><code>font_size</code> : Size of the font for editable text.</p><p><code>font_options</code> : Valid values: <code>bold</code>, <code>italic</code>, <code>no_antialias</code>, <code>gray_antialias</code>, <code>subpixel_antialias</code>, <code>directwrite</code> (Windows).</p><p><code>gutter</code> : Toggles display of gutter.</p><p><code>rulers</code> : Columns in which to display vertical rules. Accepts a list of numeric values (such as <code>[79, 89, 99]</code>) or a single numeric value (for example, <code>79</code>).</p><p><code>draw_minimap_border</code> : Set to <code>true</code> to draw a border around the minimap&#39;s region corresponding to the the view&#39;s currently visible text. The active color scheme&#39;s <code>minimapBorder</code> key controls the border&#39;s color.</p><p><code>highlight_line</code> : Set to <code>false</code> to stop highlighting lines with a cursor.</p><p><code>line_padding_top</code> : Additional spacing at the top of each line, in pixels.</p><p><code>line_padding_bottom</code> : Additional spacing at the bottom of each line, in pixels.</p><p><code>scroll_past_end</code> : Set to <code>false</code> to disable scrolling past the end of the buffer. If <code>true</code>, Sublime Text will leave a wide, empty margin between the last line and the bottom of the window.</p><p><code>line_numbers</code> : Toggles display of line numbers in the gutter.</p><p><code>word_wrap</code> : If set to <code>false</code>, long lines will be clipped instead of wrapped. Scroll the screen horizontally to see the clipped text.</p><p><code>wrap_width</code> : If greater than <code>0</code>, wraps long lines at the specified column as opposed to the window width. Only takes effect if <code>word_wrap</code> is set to <code>true</code>.</p><p><code>indent_subsequent_lines</code> : If set to <code>false</code>, wrapped lines will not be indented. Only takes effect if <code>word_wrap</code> is set to <code>true</code>.</p><p><code>draw_centered</code> : If set to <code>true</code>, text will be drawn centered rather than left-aligned.</p><p><code>match_brackets</code> : Set to <code>false</code> to disable underlining the brackets surrounding the cursor.</p><p><code>match_brackets_content</code> : Set this to <code>false</code> if you&#39;d rather have brackets highlighted only when the cursor is next to one.</p><p><code>match_brackets_square</code> : Set to <code>false</code> to stop highlighting square brackets. Only takes effect if <code>match_brackets</code> is <code>true</code>.</p><p><code>match_brackets_braces</code> : Set to <code>false</code> to stop highlighting curly brackets. Only takes effect if <code>match_brackets</code> is <code>true</code>.</p><p><code>match_brackets_angle</code> : Set to <code>false</code> to stop highlighting angle brackets. Only takes effect if <code>match_brackets</code> is <code>true</code>.</p><h3 id="automatic-behavior" tabindex="-1">Automatic Behavior <a class="header-anchor" href="#automatic-behavior" aria-label="Permalink to &quot;Automatic Behavior&quot;">​</a></h3><p><code>auto_match_enabled</code> : Toggles automatic pairing of quotes, brackets, etc.</p><p><code>save_on_focus_lost</code> : Set to true to save files automatically when switching to a different file or application.</p><p><code>find_selected_text</code> : If <code>true</code>, the selected text will be copied into the find panel when it&#39;s shown.</p><p><code>word_separators</code> : Characters considered to divide words for actions like advancing the cursor, etc. Not used for every context where a notion of a word separator is useful (for example, word wrapping). In some contexts, the text might be tokenized based on other criteria (for example, the syntax definition rules).</p><p><code>ensure_newline_at_eof_on_save</code> : Always adds a new line at the end of the file if not present when saving.</p><h3 id="system-and-miscellaneous-settings" tabindex="-1">System and Miscellaneous Settings <a class="header-anchor" href="#system-and-miscellaneous-settings" aria-label="Permalink to &quot;System and Miscellaneous Settings&quot;">​</a></h3><p><code>is_widget</code> : Returns <code>true</code> if the buffer is an input field in a dialog, as opposed to a regular buffer.</p><p><code>spell_check</code> : Toggles the spell checker.</p><p><code>dictionary</code> : Word list to be used by the spell checker. Accepts a path rooted at the data directory (such as <code>Packages/Language - English/en_US.dic</code>). You can <a href="https://extensions.services.openoffice.org/en/dictionaries" target="_blank" rel="noreferrer">add more dictionaries</a>.</p><p><code>fallback_encoding</code> : The encoding to use when the encoding can&#39;t be determined automatically. ASCII, UTF-8 and UTF-16 encodings will be detected automatically .</p><p><code>default_line_ending</code> : Determines what characters to use to designate new lines. Valid values: <code>system</code> (OS-dependant), <code>windows</code> (<code>CRLF</code>) and <code>unix</code> (<code>LF</code>).</p>',39),b=t("code",null,"tab_completion",-1),w=i('<h3 id="build-and-error-navigation-settings" tabindex="-1">Build and Error Navigation Settings <a class="header-anchor" href="#build-and-error-navigation-settings" aria-label="Permalink to &quot;Build and Error Navigation Settings&quot;">​</a></h3><p><code>result_file_regex</code> and <code>result_line_regex</code> : Regular expressions used to extract error information from some output dumped into a view or output panel. Follows the same rules as <a href="https://www.sublimetext.com/docs/build_systems.html#exec-target-options" target="_blank" rel="noreferrer">error capturing in build systems</a>.</p><p><code>result_base_dir</code> : Folder to start looking for offending files based on information extracted with <code>result_file_regex</code> and <code>result_line_regex</code>.</p><p><code>build_env</code> : List of paths to add to build systems by default.</p><h3 id="file-and-directory-settings" tabindex="-1">File and Directory Settings <a class="header-anchor" href="#file-and-directory-settings" aria-label="Permalink to &quot;File and Directory Settings&quot;">​</a></h3><p><code>default_dir</code> : Sets the default save folder for the view.</p><h3 id="input-settings" tabindex="-1">Input Settings <a class="header-anchor" href="#input-settings" aria-label="Permalink to &quot;Input Settings&quot;">​</a></h3><p><code>command_mode</code> : If set to <code>true</code>, the buffer will ignore key strokes. Useful when emulating Vim&#39;s modal behavior.</p>',8);function k(y,S,v,x,T,I){const o=d("Key");return n(),c("div",null,[l,t("p",null,[p,e(" : Determines whether to replace a tab character with "),h,e(" number of spaces when "),a(o,{k:"tab"}),e(" is pressed.")]),t("p",null,[u,e(" : If "),_,e(" is "),f,e(", will make "),a(o,{k:"tab"}),e(" and")]),a(o,{k:"backspace"}),e(" insert/delete "),g,e(" number of spaces per key press."),m,t("p",null,[b,e(" : Determines whether pressing "),a(o,{k:"tab"}),e(" will insert completions.")]),w])}const P=s(r,[["render",k]]);export{A as __pageData,P as default};
